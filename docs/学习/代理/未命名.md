智能体的设计模式
1.反射
2.工具调用
3.Planning
4.多智能体协作

1.反射
我们可以用大模型来评估并优化大模型给我们生成的结果。
尤其当我们将v1的结果与它具体的问题一起发给大模型，优化的结果会更好，比如你让大模型生成了一段代码，你自己去运行，发现有错误，那么你应该把代码和错误一起发送给大模型，这样大模型优化的结果会更好。

你也可以在prompt中加入一些few-shot示例，也是利用了反射的原理，来提升模型输出的质量。

构建一系列的标准问答对的测试集，来评估使用反射前后生成的质量

